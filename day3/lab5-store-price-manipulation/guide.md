# Lab 5: Store Price Manipulation

- Create an account. You start with `$200`.
- Pick any item and buy it normally once.
- Intercept or modify the request and change the `price` parameter before it reaches the server.
- The backend uses the client-supplied `price` when it deducts money from your balance.
- Goal: buy an expensive item for a very small amount.
